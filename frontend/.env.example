# TOITURELV Cortex Frontend - Environment Variables
# Copy this file to .env.local and fill in your values

# Backend API URL
# For local development: http://localhost:8000
# For production: Your Railway deployment URL (e.g., https://toiturelv-cortex.railway.app)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Supabase Settings (Client-side)
# Get these from https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
# Use the ANON KEY (public) - never expose the SERVICE_ROLE key to the frontend
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Iron Session Secret (Server-side only)
# Generate a secure 32+ character random string
# Example: openssl rand -hex 16
IRON_SESSION_SECRET=your_32_character_or_longer_secret

# Application Password (Server-side only)
# Shared password for team access to the application
APP_PASSWORD=your_team_password
